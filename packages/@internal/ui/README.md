# @internal/ui

> **Internal package** — not published, not intended for direct consumption.

This package contains shared UI utilities used by `@visual-yaml/react` and `@visual-yaml/vue`. All exports are internal implementation details and **not public APIs** — they may change, move, or be removed at any time without notice.

Use `@visual-yaml/react` or `@visual-yaml/vue` instead.
